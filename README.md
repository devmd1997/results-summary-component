# Results Summary Component

This project is a solution to the Results Summary Component challenge, built with React, TypeScript, and Vite. It showcases a responsive card component that displays a user's result summary and score breakdown.

## 🚀 Technologies Used

-   **React**: Library for building the user interface.
-   **TypeScript**: For static type checking and improved developer experience.
-   **Vite**: Fast frontend build tool.
-   **Storybook**: For developing and testing UI components in isolation.
-   **Vitest**: For unit testing.
-   **PostCSS & Autoprefixer**: For CSS processing and cross-browser compatibility.

## ✨ Features

-   **Component-Driven Development**: UI components were built and verified using Storybook.
-   **Responsive Design**: The layout adapts seamlessly to different screen sizes.
-   **Type Safety**: Full TypeScript support for robust code.
-   **Testing**: Unit tests setup with Vitest to ensure reliability.

## 🛠️ Getting Started

### Prerequisites

Ensure you have Node.js installed on your machine.

### Installation

1.  Clone the repository.
2.  Navigate to the project directory.
3.  Install dependencies:
    ```bash
    npm install
    ```

## 📜 Scripts

-   `npm run dev`: Starts the development server.
-   `npm run build`: Builds the app for production.
-   `npm run preview`: Locally preview the production build.
-   `npm run storybook`: Starts the Storybook server to view components.
-   `npm run test`: Runs unit tests with Vitest.

## 🤝 Contributing

Contributions are welcome! Please feel free to submit a Pull Request.